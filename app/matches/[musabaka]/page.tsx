import { notFound } from "next/navigation"; import { dataProvider } from "@/lib/data-provider"; import { DemoNotice, FormDots, Metric, TeamMark } from "@/components/ui";
export default async function MatchPage({ params }: { params: Promise<{ musabaka: string }> }) { const match = await dataProvider.getMatch((await params).musabaka); if (!match) notFound(); const p = match.prediction; const injuries = await dataProvider.getInjuries(match.home.id); return <section className="page detail"><p className="eyebrow">{match.leagueId === "super-lig" ? "SÜPER LİG" : "ŞAMPİYONLAR LİGİ"} · {match.dateLabel} · {match.kickoff}</p><div className="match-hero"><div><TeamMark team={match.home}/><h1>{match.home.name}</h1></div><div className="score-box"><small>MODEL TAHMİNİ</small><strong>{p.predictedScore}</strong><span>Güven {p.confidence.score}%</span></div><div><TeamMark team={match.away}/><h1>{match.away.name}</h1></div></div><DemoNotice/><div className="detail-grid"><article className="data-card"><h2>1-X-2 olasılıkları</h2><div className="probabilities"><span><b>1</b><strong>{p.probabilities.home}%</strong></span><span><b>X</b><strong>{p.probabilities.draw}%</strong></span><span><b>2</b><strong>{p.probabilities.away}%</strong></span></div><div className="metrics-row"><Metric label="2.5 ÜST" value={`${p.probabilities.over25}%`}/><Metric label="KG VAR" value={`${p.probabilities.btts}%`}/></div></article><article className="data-card"><h2>Skor dağılımı</h2><p className="subtle">MODELİN EN OLASI SKORU</p>{p.scoreDistribution.map((item,index) => <div className="score-bar" key={item.score}><b>{item.score}</b><i><span style={{width:`${item.probability * 4}%`}}/></i><strong>{item.probability}%</strong>{index === 0 && <em>En olası</em>}</div>)}</article><article className="data-card"><h2>Son 5 maç formu</h2><div className="comparison"><div><TeamMark team={match.home} small/><strong>{match.home.name}</strong><FormDots team={match.home}/></div><div><TeamMark team={match.away} small/><strong>{match.away.name}</strong><FormDots team={match.away}/></div></div></article><article className="data-card"><h2>Takım karşılaştırması</h2>{[["Gol",match.home.stats.goalsFor,match.away.stats.goalsFor],["xG",match.home.stats.xg.toFixed(1),match.away.stats.xg.toFixed(1)],["Yenilen gol",match.home.stats.goalsAgainst,match.away.stats.goalsAgainst],["xGA",match.home.stats.xga.toFixed(1),match.away.stats.xga.toFixed(1)]].map(([label,home,away]) => <div className="compare-row" key={String(label)}><b>{String(home)}</b><span>{label}</span><b>{String(away)}</b></div>)}</article><article className="data-card"><h2>Kadro durumu</h2>{injuries.length ? injuries.map((injury) => <p key={injury.player}><b>{injury.status}:</b> {injury.player} — {injury.note}</p>) : <p>Demo veri setinde bildirilen oyuncu durumu yok.</p>}<p><b>Muhtemel 11:</b> Altyapı hazır; resmi kadro verisi bağlandığında burada yayınlanır.</p></article><article className="data-card"><h2>Model metodolojisi</h2><p>Bu görünüm {p.methods.join(", ")} sinyallerini birleştiren bir ensemble mimarisine ayrılmıştır. Olasılık dağılımı ile model güveni ayrı hesaplanır.</p><p className="subtle">H2H ve takım haberleri, doğrulanmış veri sağlayıcısı bağlandığında gösterilecektir.</p></article></div></section>; }
