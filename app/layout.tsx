import type { Metadata } from "next"; import "./globals.css"; import { BottomNav, Footer, Header } from "@/components/site-shell"; import { ThemeProvider } from "@/components/theme-provider";
export const metadata: Metadata = { title: { default: "FOOTBALL IQ — Futbolu verilerle oku.", template: "%s | FOOTBALL IQ" }, description: "Süper Lig ve Şampiyonlar Ligi maçları için istatistiksel futbol analizleri, skor tahminleri ve olasılık tabanlı model sonuçları.", openGraph: { title: "FOOTBALL IQ — Futbolu verilerle oku.", description: "İstatistiksel futbol analizleri ve olasılık tabanlı model sonuçları.", type: "website" } };
export default function RootLayout({ children }: Readonly<{ children: React.ReactNode }>) { const structuredData = { "@context": "https://schema.org", "@type": "WebSite", name: "FOOTBALL IQ", description: "Süper Lig ve Şampiyonlar Ligi için olasılık tabanlı futbol analizi.", inLanguage: "tr-TR" }; return <html lang="tr" suppressHydrationWarning><body><ThemeProvider><Header/><main>{children}</main><Footer/><BottomNav/></ThemeProvider><script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(structuredData) }}/></body></html>; }
