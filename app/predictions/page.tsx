import { DemoNotice, MatchCard } from "@/components/ui"; import { dataProvider } from "@/lib/data-provider";
export default async function Predictions() { const matches = await dataProvider.getMatches(); const sorted = [...matches].sort((a,b) => b.prediction.confidence.score-a.prediction.confidence.score); return <section className="page"><p className="eyebrow">MODEL RADARI</p><h1>Tahminler</h1><p className="lead">Günün en güçlü model sinyallerini ve güven düzeylerini karşılaştırın.</p><DemoNotice/><h2>Yüksek güven sinyalleri</h2><div className="match-grid">{sorted.map((match) => <MatchCard key={match.id} match={match}/>)}</div></section>; }
